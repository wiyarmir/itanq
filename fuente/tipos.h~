/* 
 * File:   tipos.h
 * Author: guillermo
 *
 * Created on 21 de marzo de 2009, 19:02
 */

#ifndef _TIPOS_H
#define	_TIPOS_H
#include <cstdlib>
#ifdef DEB
#include <iostream>
using namespace std;
#endif
//Icono de la ventana
const char NOMBREAPP[] = "iTanq"; //Nombre de la ventana
const int TAM_TILE = 40; //tamaño en px
const int RES_W = 800; //y ancho de la ventana principal
const int RES_H = 600; //alto
const int NTIL_W = (int) (RES_W / TAM_TILE);
const int NTIL_H = (int) (RES_H / TAM_TILE);

typedef struct {
    int x, y;
} Punto;

typedef enum {
    ARR, ABA, IZQ, DER
} Orientacion;



#endif	/* _TIPOS_H */
